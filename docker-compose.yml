services:
  db:
    image: postgres:16
    container_name: kotoba-db
    environment:
      POSTGRES_DB: kotoba
      POSTGRES_USER: kotoba
      POSTGRES_PASSWORD: kotoba_dev_pw
    ports:
      - "5432:5432"
    volumes:
      - kotoba_pgdata:/var/lib/postgresql/data

volumes:
  kotoba_pgdata:
